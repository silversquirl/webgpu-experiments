{
  "name": "grass",
  "scripts": {
    "compile": "bun build --outdir build --target browser --loader .wgsl:text src/index.ts",
    "serve": "bunx @squirl/nova --loader=.wgsl:text"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.2.2",
    "@squirl/nova": "^0.1.5",
    "@webgpu/types": "^0.1.34"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  }
}
